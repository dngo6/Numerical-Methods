function [C] = my_multiply(A , B)
    sizeA = size(A);
    sizeB = size(B);
    C = zeros(sizeA(1), sizeB(2));
    sizeC = size(C);
        
    for i = 0: sizeC(1)
        for j = 0: sizeC(2)
            temp = 0;
            
        end
    end
end